<?xml version="1.0" encoding="UTF-8"?>
<plugin id="cordova-plugin-wish-sender"
        version="1.0.0"
        xmlns="http://apache.org/cordova/ns/plugins/1.0"
        xmlns:android="http://schemas.android.com/apk/res/android">

  <name>WishSender</name>
  <description>AlarmManager scheduling + Accessibility auto-send for WhatsApp</description>
  <license>MIT</license>

  <!-- JS bridge -->
  <js-module src="www/WishPlugin.js" name="WishPlugin">
    <clobbers target="WishPlugin" />
  </js-module>

  <!-- Android -->
  <platform name="android">
    <config-file target="res/xml/config.xml" parent="/*">
      <feature name="WishPlugin">
        <param name="android-package" value="com.wishreminder.plugin.WishPlugin" />
        <param name="onload" value="true" />
      </feature>
    </config-file>

    <!-- Permissions -->
    <config-file target="AndroidManifest.xml" parent="/manifest">
      <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
      <uses-permission android:name="android.permission.WAKE_LOCK" />
      <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
      <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
      <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
      <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
      <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
      <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE"
                       tools:ignore="ProtectedPermissions"/>
      <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
                       tools:ignore="QueryAllPackagesPermission"/>
    </config-file>

    <!-- Register components in AndroidManifest -->
    <config-file target="AndroidManifest.xml" parent="/manifest/application">

      <!-- AlarmReceiver -->
      <receiver android:name="com.wishreminder.plugin.AlarmReceiver"
                android:exported="false"
                android:enabled="true">
        <intent-filter>
          <action android:name="android.intent.action.BOOT_COMPLETED" />
        </intent-filter>
      </receiver>

      <!-- WishSenderService -->
      <service android:name="com.wishreminder.plugin.WishSenderService"
               android:exported="false"
               android:foregroundServiceType="specialUse">
        <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                  android:value="autoReply"/>
      </service>

      <!-- Accessibility Service -->
      <service android:name="com.wishreminder.plugin.WishAccessibilityService"
               android:exported="true"
               android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
               android:label="WishReminder Auto-Send">
        <intent-filter>
          <action android:name="android.accessibilityservice.AccessibilityService" />
        </intent-filter>
        <meta-data
          android:name="android.accessibilityservice"
          android:resource="@xml/accessibility_service_config" />
      </service>

    </config-file>

    <!-- Java source files -->
    <source-file src="src/android/java/com/wishreminder/plugin/WishPlugin.java"
                 target-dir="app/src/main/java/com/wishreminder/plugin" />
    <source-file src="src/android/java/com/wishreminder/plugin/AlarmReceiver.java"
                 target-dir="app/src/main/java/com/wishreminder/plugin" />
    <source-file src="src/android/java/com/wishreminder/plugin/WishSenderService.java"
                 target-dir="app/src/main/java/com/wishreminder/plugin" />
    <source-file src="src/android/java/com/wishreminder/plugin/WishAccessibilityService.java"
                 target-dir="app/src/main/java/com/wishreminder/plugin" />

    <!-- Accessibility XML config -->
    <resource-file src="src/android/res/xml/accessibility_service_config.xml"
                   target="app/src/main/res/xml/accessibility_service_config.xml" />
  </platform>

</plugin>
