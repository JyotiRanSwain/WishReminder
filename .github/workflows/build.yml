name: Build WishReminder APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-android:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      # Pinning Gradle 8.7 avoids the XmlParser error in newer versions
      - name: Setup Gradle 8.7
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.7

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          api-level: 34
          build-tools: '34.0.0'

      # Required for Cordova 12's build verification
      - name: Install Specific Build Tools 33.0.2
        run: |
          sdkmanager "build-tools;33.0.2"

      - name: Install Cordova
        run: npm install -g cordova@12.0.0

      - name: Install npm dependencies
        run: npm install

      - name: Fresh Prepare Platform and Local Plugin
        run: |
          # Wipe existing folders to prevent XML merge conflicts
          rm -rf platforms/android
          rm -rf plugins/cordova-plugin-wish-sender
          
          # Add plugin first, then platform
          cordova plugin add ./plugin
          cordova platform add android@12.0.1 --save
          cordova prepare android

      - name: Build Debug APK
        env:
          CORDOVA_ANDROID_GRADLE_DISTRIBUTION_URL: https://services.gradle.org/distributions/gradle-8.7-bin.zip
        # Using --debug ensures the APK is signed with a debug key
        run: cordova build android --debug --device

      - name: Rename and Move APK
        if: success()
        run: |
          mkdir -p output
          # Locate the signed debug APK and move it to output
          find platforms/android/app/build/outputs/apk/debug/ -name "*.apk" -exec cp {} output/wishsender.apk \;
          echo "Debug build successful. File ready at output/wishsender.apk"

      - name: Upload wishsender.apk
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: wishsender
          path: output/wishsender.apk
          retention-days: 30
